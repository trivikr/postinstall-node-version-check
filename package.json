{
  "name": "postinstall-node-version-check",
  "version": "0.1.1",
  "description": "Logs a message on postinstall if the Node.js version being used is not supported.",
  "main": "dist/index.js",
  "bin": {
    "postinstall-node-version-check": "dist/index.js"
  },
  "scripts": {
    "prepublishOnly": "tsc",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trivikr/postinstall-node-version-check.git"
  },
  "keywords": [
    "node",
    "version",
    "deprecation",
    "warning",
    "postinstall"
  ],
  "author": "Kamat, Trivikram",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/trivikr/postinstall-node-version-check/issues"
  },
  "homepage": "https://github.com/trivikr/postinstall-node-version-check#readme",
  "devDependencies": {
    "typescript": "4.3.5"
  }
}
